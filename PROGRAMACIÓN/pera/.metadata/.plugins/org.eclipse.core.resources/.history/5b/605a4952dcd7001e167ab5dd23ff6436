package EJRepaso4;

import java.time.LocalDate;
import java.util.List;

public class Libreria {

	
	private String direccion;
	private Integer telefono;
	private List<Libro> libros;
	
	
	public Libreria(String titulo, String autor, LocalDate fechaEdicion, Integer paginas) {
		libros = new ArrayList<>;
	}
	
	
	public Libro obtenerLibroMasAntiguo() {
		if(!this.libros.isEmpty()) {
			Libro old= this.libros.get(0);
			for(Libro libro: this.libros) {
				if(libro.getFechaEdicion().isBefore(old.getFechaEdicion())) {
					old=libro;
				}
			}
			return old;
		}
		return null;
	}
	
	public Integer obtenerPesoTotalDeLibros() {
		Integer pesoTotal=0;
		for(Libro libro:this.libros) {
			pesoTotal+=libro.getPeso();
		}
		return pesoTotal;
	}
	
	public Integer obtenerNumPaginasMedio() {
		Integer total=0;
		if(this.libros.isEmpty()) {
			return 0;
		}
		for(Libro libro:this.libros) {
			total+=libro.getPaginas();
		}
		return total/this.libros.size();
	
	
	
	
	
	
	
	
	
	
	
}
